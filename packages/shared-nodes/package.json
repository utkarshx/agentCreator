{
  "name": "@agent-creator/shared-nodes",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
    "scripts": {
    "build": "rm -rf dist && mkdir -p dist/nodes && cp src/nodes/*.js dist/nodes/ && cp src/index.js dist/ 2>/dev/null || true",
    "dev": "echo 'Shared nodes in dev mode - using src files directly'",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@comfyorg/litegraph": "^0.7.6"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "@types/node": "^20.0.0"
  },
  "files": [
    "dist/**/*"
  ]
}